{
  "name": "ai-music-collaborator",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "api",
    "workers",
    "infra",
    "docs"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "docker:build": "turbo run docker:build",
    "docker:push": "turbo run docker:push"
  },
  "devDependencies": {
    "turbo": "^1.12.4",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
